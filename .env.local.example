# Supabase Configuration for Arz. Landing Page
# Replace these values with your actual Supabase project credentials

NEXT_PUBLIC_SUPABASE_URL=https://your-supabase-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# SQL Migration Script for Supabase SQL Editor:
# ----------------------------------------------------
# CREATE TABLE community_members (
#   id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
#   name TEXT NOT NULL,
#   email TEXT NOT NULL,
#   city TEXT NOT NULL,
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
# );
#
# Enable Row Level Security (RLS) or add an insert policy:
# ALTER TABLE community_members ENABLE ROW LEVEL SECURITY;
# CREATE POLICY "Allow public insert" ON community_members FOR INSERT WITH CHECK (true);
